=== Tuned Deployment Report ===
Host: {{ inventory_hostname }}
Date: {{ ansible_date_time.iso8601 }}

Active Profile:
{{ lookup('pipe', 'tuned-adm active') }}

Key sysctl values:
vm.swappiness: {{ lookup('pipe', 'sysctl -n vm.swappiness') }}
vm.dirty_ratio: {{ lookup('pipe', 'sysctl -n vm.dirty_ratio') }}
net.core.rmem_max: {{ lookup('pipe', 'sysctl -n net.core.rmem_max') }}
net.core.wmem_max: {{ lookup('pipe', 'sysctl -n net.core.wmem_max') }}
fs.file-max: {{ lookup('pipe', 'sysctl -n fs.file-max') }}

CPU Governor:
{{ lookup('pipe', 'cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor 2>/dev/null || echo N/A') }}
